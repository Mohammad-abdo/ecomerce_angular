import { ImagStyleDirective } from './imag-style.directive';

describe('ImagStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagStyleDirective();
    expect(directive).toBeTruthy();
  });
});
